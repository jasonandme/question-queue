# Security Policy

## 报告安全问题

请不要在公开 issue 中提交可利用的安全漏洞、真实 API Key、私人对话或账号数据。优先使用 GitHub 的 Private vulnerability reporting；如果仓库尚未启用该功能，请只提交一个不包含漏洞细节的 issue，请求维护者提供私密联系方式。

报告可以包含受影响版本、攻击前提、影响范围、使用虚构数据的最小复现和建议修复方向。

## 密钥处理

仓库不会提供或接受任何内置模型密钥。用户自行配置的千问 API Key 保存在浏览器本地存储中，只适合个人使用。发布公共服务时应改用服务端代理、短期令牌、用户级额度和撤销机制。

如果密钥曾出现在提交、issue、日志或截图中，应立即在服务商控制台撤销；仅删除公开文本不能使已经泄露的密钥恢复安全。

## 支持范围

安全修复优先应用于当前主分支和最新 Release。旧版本可能不会单独回补。

