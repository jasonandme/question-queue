# Privacy

Question Queue 采用本地优先设计。扩展本身不运营同步服务器，不包含广告、分析、遥测或错误追踪 SDK。

## 本地保存的数据

以下数据保存在浏览器的 chrome.storage.local：

- 问题、相关上下文和学习笔记。
- 标签与待输入、已输入、已回答、已掌握状态。
- 模型平台、页面标题、来源链接和对话标识。
- 最近一次生成的思维导图。
- 用户自行配置的千问 Base URL 和 API Key。

删除扩展通常会删除这些本地数据。卸载前应先导出 JSON 备份。

## 外部传输

默认记录、筛选、填入、JSON 备份和 Word 导出均在本机完成。

只有用户主动点击“生成思维导图”时，扩展才会向用户配置的阿里云百炼接口发送：

- 问题文本。
- 问题状态。
- 匿名问题 ID。

该请求不包含相关上下文、学习笔记、来源网址、对话标题或完整模型回答。阿里云如何处理请求数据取决于用户与阿里云之间的服务条款和账户设置。

## API Key

API Key 保存在浏览器扩展本地存储中，不会写入 JSON 备份或 Word 文档。浏览器本地存储不是专业密钥保险库，建议使用独立、有限额度、可随时撤销的 Key。不要将密钥写入源码、issue、日志或截图。

## 网站访问

扩展只在 manifest 中列出的模型站点注入内容脚本，用于显示快捷入口、识别输入框和追加用户选中的问题。扩展不会收集账号密码，也不会主动上传完整对话历史。