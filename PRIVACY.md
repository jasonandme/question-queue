# Privacy

追问簿（Question Queue）采用本地优先设计。扩展本身不运营同步服务器，不包含广告、分析、遥测或错误追踪 SDK。

## 本地保存的数据

以下数据保存在浏览器的 chrome.storage.local：

- 问题、相关上下文和学习笔记。
- 笔记正文、自动识别的章节标题、标签与笔记/待输入/已输入/已回答/已掌握状态。
- 模型平台、来源类型、页面标题、来源链接和对话标识。
- 标记“已回答”时所在页面的链接，用于回看答案位置。
- 最近一次生成的思维导图。
- 用户自行配置的千问 Base URL、模型名称和 API Key。
- 界面偏好，例如批量填入的开头提示语模板。

删除扩展通常会删除这些本地数据。卸载前应先导出 JSON 备份。

## 外部传输

默认记录、筛选、填入、JSON 备份和 Word 导出均在本机完成。

只有用户主动点击“生成思维导图”时，扩展才会向用户配置的阿里云百炼接口发送：

- 问题文本。
- 问题状态。
- 匿名问题 ID。

该请求不包含相关上下文、学习笔记、来源网址、回答页面链接、对话标题或完整模型回答。阿里云如何处理请求数据取决于用户与阿里云之间的服务条款和账户设置。

## API Key

API Key 保存在浏览器扩展本地存储中，不会写入 JSON 备份或 Word 文档。浏览器本地存储不是专业密钥保险库，建议使用独立、有限额度、可随时撤销的 Key。不要将密钥写入源码、issue、日志或截图。

## 导入数据

导入 JSON 时，扩展会按字段白名单重建每条记录，丢弃未知字段，并且只保留 http 或 https 链接。这样可以避免备份文件中的可执行链接被写入本地存储并渲染成可点击元素。

## 网站访问

扩展只在 manifest 中列出的大模型、CSDN 和知乎域名注入内容脚本。大模型站点用于显示快捷入口、识别输入框和追加用户选中的问题；CSDN、知乎用于显示快捷入口并读取当前页面的标题、canonical URL 和用户主动选择的文字。

扩展不会读取账号密码，不会抓取整篇文章，也不会主动上传完整文章、完整对话历史或页面浏览记录。
